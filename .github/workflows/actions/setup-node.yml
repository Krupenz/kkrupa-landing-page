name: 'Setup Node.js Environment'
description: 'Checks out repository and sets up Node.js'

runs:
    using: 'composite'
    steps:
        - name: Setup Node.js
          uses: actions/setup-node@v4
          with:
              node-version: '22.x'
              cache: 'npm'
